import { Spinner } from '@chakra-ui/react'
import React from 'react'

const Loading = () => {
  return (
    <div className='flex justify-center items-center | w-full h-full'><Spinner/></div>
  )
}

export default Loading